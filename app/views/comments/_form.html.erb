<% if @post %>
    <%= simple_form_for [@post, @post.comments.build] do |f| %>

        <%= f.input :body %>
        <%= f.submit "Post comment", class: 'btn btn-outline-primary' %>

    <% end %>
<%else %>
    <p>Post not found.</p>
<%end %>